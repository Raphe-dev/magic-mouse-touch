//  ContentView.swift
//  Retained for Xcode preview compatibility; the live UI is SettingsView
//  presented inside a MenuBarExtra window.

import SwiftUI

struct ContentView: View {
    var body: some View {
        SettingsView()
    }
}

#Preview {
    ContentView()
}
